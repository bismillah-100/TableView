name: Build and Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build archive
        run: |
          xcodebuild archive \
            -scheme TableView \
            -archivePath build/TableView.xcarchive \
            -configuration Release \
            -destination "generic/platform=macOS"

      - name: Extract .app from archive
        run: |
          mkdir -p build
          cp -R build/TableView.xcarchive/Products/Applications/TableView.app build/

      - name: Zip .app
        run: |
          cd build
          zip -r TableView.zip TableView.app

      - name: Upload Release Asset
        uses: softprops/action-gh-release@v1
        with:
          files: build/TableView.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
